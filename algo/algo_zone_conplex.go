package algo

type node struct {
	val  int
	next *node
}

func newNode(val int) *node {
	return &node{val: val}
}

func function() int {
	return 0
}

func Algorithm(a int) int {
	const name = 0
	b := 0
	newNode(0)
	c := function()
	return a + b + c
}
